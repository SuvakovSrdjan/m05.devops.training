apiVersion: v1
kind: Service
metadata:
  name: flask-app
  labels:
    app: flask-app
spec:
  selector:
    app: flask-app
    version: blue
  ports:
    - name: http
      port: 75
      targetPort: 4000
      nodePort: 30049
  type: NodePort
